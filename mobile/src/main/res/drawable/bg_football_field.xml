<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Field Background -->
    <item>
        <shape android:shape="rectangle">
            <solid android:color="@color/team_electric_green" />
            <corners android:radius="20dp" />
        </shape>
    </item>

    <!-- Field Border -->
    <item>
        <shape android:shape="rectangle">
            <stroke
                android:width="4dp"
                android:color="@color/stencil_white" />
            <corners android:radius="20dp" />
            <solid android:color="@android:color/transparent" />
        </shape>
    </item>

    <!-- Center Line (Horizontal) -->
    <item android:gravity="center_vertical">
         <shape android:shape="line">
            <stroke
                android:width="2dp"
                android:color="@color/stencil_white"
                android:dashWidth="10dp"
                android:dashGap="10dp" />
            <size android:height="2dp" />
        </shape>
    </item>

    <!-- Center Circle (Optional, for realism) -->
    <item android:width="60dp" android:height="60dp" android:gravity="center">
        <shape android:shape="oval">
            <stroke android:width="2dp" android:color="@color/stencil_white" />
            <solid android:color="@android:color/transparent" />
        </shape>
    </item>

</layer-list>