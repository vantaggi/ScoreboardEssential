plugins {
    alias(libs.plugins.kotlin.jvm)
}

// Kotlin puro, ZERO Android. Tre ragioni concrete:
//  - i test qui sono garantiti di essere eseguiti (in :wear la suite e' rimasta
//    silenziosamente saltata per anni, e in :mobile serve Robolectric);
//  - le regole di uno sport sono logica pura e non hanno bisogno di un emulatore;
//  - nessuna @Entity ne' @Parcelize qui dentro, quindi il modulo non riapre il
//    blocco verso AGP 9.
kotlin {
    jvmToolchain(17)
}

dependencies {
    testImplementation libs.junit4
}
